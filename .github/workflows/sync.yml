name: Sync to Organization

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  sync:
    uses: iiiiiei/github-actions/.github/workflows/sync-core.yml@main
    with:
      target_repo: Farverge/DSH-MacOS
    secrets:
      TARGET_PAT: ${{ secrets.FARVERGE_ORG_TOKEN }}
